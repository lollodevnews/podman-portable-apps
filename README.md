# podman-portable-apps
A rootless container manager to build, run, and export Linux GUI applications as single, portable .tar files. Features granular permission control (network isolation, hardware access) and persistent session snapshots (extensions/history)
